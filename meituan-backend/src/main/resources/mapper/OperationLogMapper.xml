<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meituan.product.mapper.OperationLogMapper">
    
    <!-- 根据商家ID查询操作日志 -->
    <select id="selectByMerchantId" resultType="com.meituan.product.entity.OperationLog">
        SELECT * FROM t_operation_log
        WHERE merchant_id = #{merchantId}
        AND deleted = 0
        ORDER BY created_time DESC
    </select>
    
    <!-- 根据操作类型查询日志 -->
    <select id="selectByOperationType" resultType="com.meituan.product.entity.OperationLog">
        SELECT * FROM t_operation_log
        WHERE merchant_id = #{merchantId}
        AND operation_type = #{operationType}
        AND deleted = 0
        ORDER BY created_time DESC
    </select>
    
</mapper>

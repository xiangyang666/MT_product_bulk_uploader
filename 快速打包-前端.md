# 快速打包 - 前端

## 🚀 Web 应用打包

### 一键打包
```bash
build-frontend.bat
```

### 手动打包
```bash
cd meituan-frontend
npm install
npm run build
```

**输出目录：** `meituan-frontend/dist/`

---

## 🖥️ Electron 桌面应用打包

### 一键打包
```bash
build-electron.bat
```

### 手动打包
```bash
cd meituan-frontend
npm install
npm run electron:build:win
```

**输出目录：** `meituan-frontend/dist-electron/`

---

## 📦 部署 Web 应用

### 方式 1：上传到服务器

```bash
# 1. 压缩
cd meituan-frontend
tar -czf dist.tar.gz dist/

# 2. 上传
scp dist.tar.gz root@106.55.102.48:/opt/meituan/

# 3. 解压
ssh root@106.55.102.48
cd /opt/meituan/
tar -xzf dist.tar.gz
```

### 方式 2：配置 Nginx

```nginx
server {
    listen 80;
    root /opt/meituan/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

---

## ✅ 验证

1. 打开浏览器访问应用
2. 测试登录功能（admin/admin123）
3. 检查 API 连接是否正常

---

## 🔧 配置

### API 地址配置

**文件：** `meituan-frontend/.env.production`

```env
VITE_API_BASE_URL=http://106.55.102.48:8080
```

修改后需要重新打包。

---

## 📞 常见问题

**问题：npm install 失败**
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

**问题：API 请求失败**
- 检查 `.env.production` 中的 API 地址
- 确保后端服务已启动

**问题：页面刷新 404**
- 配置 Nginx 的 `try_files`

---

**打包时间：** 2-5 分钟  
**状态：** ✅ 就绪

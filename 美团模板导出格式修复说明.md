# 美团模板导出格式修复说明

## 问题描述

导出的美团模板数据格式不符合美团平台要求，导致上传时出现以下错误：

1. **无理由退货字段**：需要填写标签ID，不是 0/1 或"是/否"
2. **售卖状态字段**：值不是 0 或 1（可能输出了"上架/下架"等文本）
3. **可售时间字段**：时间格式错误，需要支持多种格式

## 最新修复内容（2026-03-04）

### 1. 无理由退货字段修复

**问题**: 美团要求填写标签ID，不是简单的 0/1

**修复逻辑**:
- 根据数据库中的 `noReasonReturn` 值（0或1）映射到对应的标签ID
- 0 → `1300030895`（不支持7天无理由退货）
- 1 → `1300030901`（7天无理由退货）

**可用的标签ID**:
- `1300030895` - 不支持7天无理由退货
- `1300030901` - 7天无理由退货（默认）
- `1300030902` - 7天无理由退货(一次性包装破损不支持)
- `1300030903` - 7天无理由退货(激活后不支持)
- `1300030904` - 7天无理由退货(使用后不支持)
- `1300030905` - 7天无理由退货(安装后不支持)
- `1300030906` - 7天无理由退货(定制类不支持)

```java
String returnTagId = "1300030895"; // 默认不支持
if (product.getNoReasonReturn() != null && product.getNoReasonReturn() == 1) {
    returnTagId = "1300030901"; // 支持7天无理由退货
}
cell.setCellValue(returnTagId);
```

### 2. 可售时间字段修复

**问题**: 美团支持多种可售时间格式，之前只支持简单的时间段

**支持的格式**:
1. **指定时间点开售**: `HH:mm`（如 `09:00`）
2. **指定时间段售卖**: `HH:mm-HH:mm`（如 `09:00-22:00`）
3. **营业全时段可售**: `营业全时段可售`
4. **周期性可售**: `时间段1，时间段2;周一，周二`（用中文分号隔开）
5. **不填写**: 默认为`营业全时段可售`

**修复逻辑**:
```java
String timeValue = "营业全时段可售"; // 默认值

if (product.getAvailableTime() != null && !product.getAvailableTime().trim().isEmpty()) {
    String time = product.getAvailableTime().trim();
    
    // 支持"全天"或"营业全时段可售"
    if ("全天".equals(time) || "营业全时段可售".equals(time)) {
        timeValue = "营业全时段可售";
    }
    // 支持时间点：HH:mm
    else if (time.matches("^(0?[0-9]|1[0-9]|2[0-3]):([0-5][0-9])$")) {
        timeValue = time;
    }
    // 支持时间段：HH:mm-HH:mm
    else if (time.matches("^(0?[0-9]|1[0-9]|2[0-3]):([0-5][0-9])-(0?[0-9]|1[0-9]|2[0-3]):([0-5][0-9])$")) {
        timeValue = time;
    }
    // 支持周期性可售：包含中文分号
    else if (time.contains("；") || time.contains(";")) {
        timeValue = time;
    }
}
cell.setCellValue(timeValue);
```

**周期性可售示例**:
- `09:00-12:00，14:00-18:00；周一，周三，周五`
- `10:00-22:00；周一，周二，周三，周四，周五`

### 1. 售卖状态字段修复

**修改位置**: `ExcelService.java` - `fillCellByHeaderName()` 方法

**修复逻辑**:
- 如果数据库中存储的是 "0" 或 "1"，直接使用
- 如果是 "上架"、"在售"、"销售中"，转换为 "1"
- 如果是 "下架"、"停售"、"已下架"，转换为 "0"
- 其他情况默认为 "1"（上架）

```java
if (product.getSaleStatus() != null) {
    String status = product.getSaleStatus().trim();
    if ("0".equals(status) || "1".equals(status)) {
        cell.setCellValue(status);
    } else if ("上架".equals(status) || "在售".equals(status) || "销售中".equals(status)) {
        cell.setCellValue("1");
    } else if ("下架".equals(status) || "停售".equals(status) || "已下架".equals(status)) {
        cell.setCellValue("0");
    } else {
        cell.setCellValue("1"); // 默认上架
    }
} else {
    cell.setCellValue("1"); // 默认上架
}
```

### 2. 无理由退货字段修复

**修改位置**: `ExcelService.java` - `fillCellByHeaderName()` 方法

**修复逻辑**:
- 直接输出数据库中的 Integer 值（0 或 1）
- 不再转换为"是/否"文本
- 默认值为 "0"（不支持无理由退货）

```java
if (product.getNoReasonReturn() != null) {
    cell.setCellValue(product.getNoReasonReturn().toString());
} else {
    cell.setCellValue("0");
}
```

### 3. 可售时间字段修复

**修改位置**: `ExcelService.java` - `fillCellByHeaderName()` 方法

**修复逻辑**:
- 验证时间格式是否符合 `HH:mm-HH:mm` 格式
- 支持 "全天" 或 "00:00-23:59" 格式
- 格式不正确时，默认设置为 "00:00-23:59"（全天）

```java
if (product.getAvailableTime() != null && !product.getAvailableTime().trim().isEmpty()) {
    String time = product.getAvailableTime().trim();
    if (time.matches("\\d{1,2}:\\d{2}-\\d{1,2}:\\d{2}") || 
        "全天".equals(time) || 
        "00:00-23:59".equals(time)) {
        cell.setCellValue(time);
    } else {
        cell.setCellValue("00:00-23:59"); // 格式不正确，设置为全天
    }
} else {
    cell.setCellValue("00:00-23:59"); // 默认全天
}
```

### 4. 其他布尔字段统一修复

以下字段也从"是/否"改为输出 0/1：

- **力荐**
- **组合商品**
- **四轮配送**
- **是否临期**
- **是否过期**
- **违规下架**
- **必填信息缺失**

**修复逻辑**:
```java
if (product.getFieldName() != null) {
    cell.setCellValue(product.getFieldName().toString());
} else {
    cell.setCellValue("0");
}
```

## 美团字段格式要求

### 布尔类型字段（必须是 0 或 1）

| 字段名 | 值 | 说明 |
|--------|-----|------|
| 售卖状态 | 0 或 1 | 0-下架，1-上架 |
| 无理由退货 | 0 或 1 | 0-不支持，1-支持 |
| 力荐 | 0 或 1 | 0-否，1-是 |
| 组合商品 | 0 或 1 | 0-否，1-是 |
| 四轮配送 | 0 或 1 | 0-否，1-是 |
| 是否临期 | 0 或 1 | 0-否，1-是 |
| 是否过期 | 0 或 1 | 0-否，1-是 |
| 违规下架 | 0 或 1 | 0-否，1-是 |
| 必填信息缺失 | 0 或 1 | 0-否，1-是 |

### 时间格式字段

| 字段名 | 格式 | 示例 |
|--------|------|------|
| 可售时间 | HH:mm-HH:mm | 09:00-22:00 |
| 可售时间 | 全天 | 全天 |
| 可售时间 | 00:00-23:59 | 00:00-23:59 |

## 测试验证

### 测试步骤

1. 导入商品数据到系统
2. 选择商品生成美团模板
3. 下载生成的 Excel 文件
4. 检查以下字段的值：
   - 售卖状态：应该是 0 或 1
   - 无理由退货：应该是 0 或 1
   - 可售时间：应该是 HH:mm-HH:mm 格式
   - 其他布尔字段：应该是 0 或 1

### 预期结果

- ✅ 所有布尔类型字段输出 0 或 1
- ✅ 售卖状态正确转换为 0 或 1
- ✅ 可售时间格式符合要求
- ✅ 上传到美团平台不再报格式错误

## 部署说明

### 后端部署

```bash
cd meituan-backend
mvn clean package -DskipTests
# 重启后端服务
```

### 验证部署

1. 重启后端服务后，生成新的美团模板
2. 检查导出的 Excel 文件中的字段格式
3. 尝试上传到美团平台验证

## 注意事项

1. **数据库数据清理**：如果数据库中已有不符合格式的数据，建议清理或转换
2. **导入验证**：在导入商品时，应该验证这些字段的格式
3. **默认值**：所有布尔字段的默认值都是 0（否）
4. **售卖状态默认值**：默认为 1（上架）
5. **可售时间默认值**：默认为 "00:00-23:59"（全天）

## 相关文件

- `meituan-backend/src/main/java/com/meituan/product/service/ExcelService.java`
- `meituan-backend/src/main/java/com/meituan/product/entity/Product.java`

## 修复日期

2026-02-26

## 修复人员

Kiro AI Assistant

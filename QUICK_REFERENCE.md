# 快速参考指南

## 🎯 项目核心功能

这是一个**美团商品批量上传管理工具**，帮助商家：
1. 导入商品类目信息
2. 自动生成美团上传模板
3. 批量上传商品到美团
4. 一键清空美团商品

## 🚀 快速启动

### Windows 系统
```bash
# 一键启动（推荐）
start-all.bat

# 或分别启动
start-backend.bat      # 后端服务
start-frontend.bat     # 桌面应用
```

### 手动启动
```bash
# 后端（需要 Maven）
cd meituan-backend
mvn spring-boot:run

# 前端（需要 Node.js 和 pnpm）
cd meituan-frontend
pnpm install           # 首次运行
pnpm run electron:dev
```

## 📋 使用流程

### 1. 导入商品
```
打开应用 → 批量导入 → 下载模板 → 填写数据 → 上传文件 → 预览 → 确认导入
```

### 2. 生成美团模板
```
商品管理 → 选择商品 → 生成模板 → 下载文件 → 上传到美团后台
```

### 3. 批量上传
```
商品管理 → 选择商品 → 批量上传 → 输入 Token → 确认上传
```

### 4. 清空商品
```
商品管理 → 一键清空 → 确认操作
```

## 🎨 界面特点

### 窗口控制（右上角）
- 🟡 **黄色圆点**：最小化窗口
- 🟢 **绿色圆点**：最大化/还原窗口
- 🔴 **红色圆点**：关闭窗口

### 快捷键
- `Ctrl+Shift+D`：打开开发者工具

### 菜单
- 首页：统计数据
- 商品管理：商品列表
- 批量导入：导入商品
- 模板管理：模板配置
- 操作日志：操作记录
- 系统设置：系统配置

## 📁 重要文件

### 配置文件
- `meituan-backend/src/main/resources/application.yml` - 后端配置
- `meituan-frontend/vite.config.js` - 前端配置

### 启动脚本
- `start-all.bat` - 一键启动
- `start-backend.bat` - 启动后端
- `start-frontend.bat` - 启动前端

### 资源文件
- `资源/商品的类目信息.csv` - 商品类目数据
- `资源/批量创建模板_v1.3.6.csv` - 美团模板格式

## 🔧 常见问题

### Q1: 如何修改后端端口？
A: 编辑 `meituan-backend/src/main/resources/application.yml`
```yaml
server:
  port: 8080  # 修改这里
```

### Q2: 如何修改数据库配置？
A: 编辑 `meituan-backend/src/main/resources/application.yml`
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meituan_product
    username: root
    password: your_password
```

### Q3: 前端如何连接后端？
A: 前端默认连接 `http://localhost:8080/api`
修改 `meituan-frontend/src/api/index.js` 中的 `baseURL`

### Q4: 如何打包应用？
A: 
```bash
# 前端打包
cd meituan-frontend
pnpm run electron:build

# 后端打包
cd meituan-backend
mvn clean package
```

### Q5: 窗口控制按钮不工作？
A: 确保 `electron/preload.js` 使用 CommonJS 语法（require）

### Q6: Excel 导入失败？
A: 检查：
- 文件格式（.xlsx 或 .xls）
- 文件大小（< 10MB）
- 必填字段是否完整
- 类目ID是否为10位数字

## 📊 数据格式

### 导入模板格式
| 字段 | 必填 | 格式 | 示例 |
|------|------|------|------|
| 商品名称 | ✅ | 文本(≤255字) | iPhone 15 Pro |
| 类目ID | ✅ | 10位数字 | 2000010001 |
| 价格 | ✅ | 数字(>0) | 7999.00 |
| 库存 | ❌ | 整数 | 100 |
| 商品描述 | ❌ | 文本(≤1000字) | 全新苹果手机 |
| 图片URL | ❌ | URL(≤500字) | https://... |

### 商品状态
- `0` - 待上传
- `1` - 已上传
- `2` - 上传失败

## 🔗 API 接口

### 后端接口（默认：http://localhost:8080/api）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/products/stats` | GET | 获取统计数据 |
| `/products/download-template` | GET | 下载模板 |
| `/products/preview` | POST | 预览数据 |
| `/products/import` | POST | 导入商品 |
| `/products` | GET | 获取商品列表 |
| `/products/{id}` | GET | 获取商品详情 |
| `/products/generate-template` | POST | 生成美团模板 |
| `/products/upload` | POST | 批量上传 |
| `/products/clear` | DELETE | 清空商品 |

## 📝 开发提示

### 前端开发
```bash
cd meituan-frontend
pnpm run dev          # 浏览器模式（开发）
pnpm run electron:dev # Electron 模式（开发）
pnpm run build        # 构建
pnpm run electron:build # 打包应用
```

### 后端开发
```bash
cd meituan-backend
mvn spring-boot:run   # 运行
mvn clean package     # 打包
mvn test              # 测试
```

### 调试技巧
1. **前端调试**：按 `Ctrl+Shift+D` 打开开发者工具
2. **后端调试**：查看控制台日志
3. **网络调试**：开发者工具 → Network 标签

## 🎓 技术栈

### 前端
- Electron 28+
- Vue 3
- Vite 5
- Element Plus
- Pinia
- Vue Router
- Axios

### 后端
- Spring Boot 3
- MyBatis Plus
- MySQL 8
- Apache POI
- Lombok
- Spring Retry

## 📚 相关文档

- `PROJECT_IMPLEMENTATION_STATUS.md` - 完整实现状态
- `CONTEXT_TRANSFER_SUMMARY.md` - 上下文转移总结
- `需求.md` - 原始需求文档

## 💡 提示

1. **首次运行**：需要先启动后端，再启动前端
2. **数据库**：需要先创建数据库和表
3. **美团 API**：需要配置真实的 API 地址和 Token
4. **文件上传**：支持拖拽上传，更方便
5. **批量操作**：建议每批不超过 500 条商品

## 🆘 获取帮助

如果遇到问题：
1. 查看控制台错误信息
2. 检查配置文件
3. 查看相关文档
4. 联系开发人员

---

**快速参考版本**：v1.0
**最后更新**：2026年2月9日

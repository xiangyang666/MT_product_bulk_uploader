# 快速实施指南 - 美团完整字段扩展

## 📋 概述

本指南将帮助你快速完成美团商品完整字段的扩展，包括：
- ✅ 数据库扩展（50+字段）
- ✅ 删除功能（批量+单个）
- ✅ 导入导出支持所有字段

---

## 🚀 第一步：数据库迁移（5分钟）

### 1. 备份现有数据（重要！）

```sql
-- 连接到MySQL
mysql -h 106.55.102.48 -u root -p

-- 备份数据库
mysqldump -h 106.55.102.48 -u root -p meituan_product > meituan_product_backup_20260209.sql

-- 或者在MySQL中备份表
USE meituan_product;
CREATE TABLE t_product_backup_20260209 AS SELECT * FROM t_product;
```

### 2. 执行迁移脚本

```sql
-- 方式A：在MySQL客户端中执行
USE meituan_product;
source database-migration-full-fields.sql

-- 方式B：命令行执行
mysql -h 106.55.102.48 -u root -p meituan_product < database-migration-full-fields.sql
```

### 3. 验证迁移结果

```sql
-- 查看表结构
DESC t_product;

-- 统计字段数量（应该有60+个字段）
SELECT COUNT(*) AS total_columns 
FROM INFORMATION_SCHEMA.COLUMNS 
WHERE TABLE_SCHEMA = 'meituan_product' 
  AND TABLE_NAME = 't_product';

-- 查看现有数据
SELECT * FROM t_product LIMIT 5;
```

---

## 📝 第二步：更新后端代码

由于改动较大，我建议分阶段实施：

### 阶段1：先实现删除功能（简单，立即可用）

#### 1.1 添加删除接口

在 `ProductController.java` 中添加：

```java
/**
 * 删除单个商品
 */
@DeleteMapping("/{id}")
public Result<Void> deleteProduct(@PathVariable Long id) {
    productService.removeById(id);
    return Result.success();
}

/**
 * 批量删除商品
 */
@DeleteMapping("/batch")
public Result<Void> batchDeleteProducts(@RequestBody List<Long> ids) {
    productService.removeByIds(ids);
    return Result.success();
}
```

#### 1.2 更新前端Products.vue

添加删除按钮和批量选择功能（见下一步）

### 阶段2：扩展字段支持（复杂，需要时间）

这部分改动较大，包括：
- 更新Product实体类（添加50+个字段）
- 更新FormatDetector（添加字段映射）
- 更新MeituanFormatParser（解析新字段）
- 更新ExcelService（导入导出新字段）

**建议：先完成删除功能，再逐步添加字段支持**

---

## 🎨 第三步：更新前端页面

### 3.1 添加删除功能到Products.vue

<function_calls>
<invoke name="strReplace">
<parameter name="path">meituan-frontend/src/views/Products.vue